/** @jsx jsx */
import { jsx, css } from '@emotion/core';
import React from 'react';
import HotlineContainer from '../containers/HotlineContainer';

export default () => {
  return (
    <div css={{ width: '100%' }}>
      <HotlineContainer />
    </div>
  );
}
